# 📘 Unity Editor Quick Reference

## 🖥️ Windows Editor Quick Reference

### 🔲 Standard Unity View (Window > Layouts > 2 by 3)

1. **Scene View**  
   Cho phép bạn điều hướng thế giới 3D, chọn và thao tác các asset trong không gian.

2. **Game View**  
   Khi nhấn **Play**, bạn sẽ biên dịch và xem trước gameplay. Mặc định hiển thị camera đang hoạt động.

3. **Hierarchy Panel**  
   Danh sách phân cấp các đối tượng trong game. Có thể chọn, đổi tên, xóa (Shift+Delete).

4. **Project Panel**  
   Hiển thị tất cả asset trong thư mục `Assets`.

5. **Inspector**  
   Cho phép xem và chỉnh sửa các component của đối tượng được chọn.

---

### 🎮 Scene View Controls
- **Toggle Full Screen Panel** = `Shift + Spacebar`  
- **Rotate** = `Alt + Left Mouse Button`  
- **Panning** = `Alt + Middle Mouse Button`  
- **Zooming** = `Alt + Right Mouse Button`

---

### 🧩 Object Manipulation
- Click để chọn object trong Scene hoặc Hierarchy  
- **Q** = Pan view  
- **W** = Move/Translate tool  
- **Ctrl + Drag** = Snapping  
- **Shift + Ctrl** = Snap to Surface  
- **Hold V** = Vertex Snapping  
- **E** = Rotate tool  
- **Shift + Ctrl** = Look At Rotation  
- **R** = Scale tool  
- **F** = Frame select (zoom vào object)  
- **Shift + Delete** = Xóa object  

---

### 📷 Camera Manipulation
- Di chuyển scene đến góc nhìn mong muốn  
- Chọn camera  
- Vào **GameObject → Align with View**  
- Có thể di chuyển camera như một object

---

### 📚 Essential Unity Concepts
- **Project** = Thư mục chứa toàn bộ game và asset  
- **Assets** = Model, hình ảnh, âm thanh, script, prefab...  
- **Packages** = Bộ asset được biên dịch sẵn  
- **GameObject** = Mọi đối tượng trong game, là "container" cho component  
- **Components** = Các thành phần chức năng của GameObject (Transform, Light, Script...)  
- **Scenes** = Nơi đặt GameObject để tạo level  
- **Prefab** = GameObject tái sử dụng, lưu trong Project View, có thể thêm nhiều lần vào scene  
- **Build** = Phiên bản xuất ra để chạy trên nền tảng cụ thể (Build Settings)

---

## 🍎 Mac OS Editor Quick Reference

### 🔲 Standard Unity View (Window > Layouts > 2 by 3)

1. **Scene View**  
   Cho phép điều hướng thế giới 3D, chọn và thao tác asset.

2. **Game View**  
   Khi nhấn **Play**, biên dịch và xem trước gameplay. Mặc định hiển thị camera đang hoạt động.

3. **Hierarchy Panel**  
   Danh sách phân cấp các đối tượng trong game. Có thể chọn, đổi tên, xóa (Command+Delete).

4. **Project Panel**  
   Hiển thị tất cả asset trong thư mục `Assets`.

5. **Inspector**  
   Cho phép xem và chỉnh sửa các component của đối tượng được chọn.

---

### 🎮 Scene View Controls
- **Toggle Full Screen Panel** = `Shift + Spacebar`  
- **Rotate** = `Alt + Left Mouse Button`  
- **Panning** = `Alt + Middle Mouse Button`  
- **Zooming** = `Alt + Right Mouse Button`

---

### 🧩 Object Manipulation
- Click để chọn object trong Scene hoặc Hierarchy  
- **Q** = Pan view  
- **W** = Move/Translate tool  
- **Command + Drag** = Snapping  
- **Shift + Command** = Snap to Surface  
- **Hold V** = Vertex Snapping  
- **E** = Rotate tool  
- **Shift + Command** = Look At Rotation  
- **R** = Scale tool  
- **F** = Frame select (zoom vào object)  
- **Command + Delete** = Xóa object  

---

### 📷 Camera Manipulation
- Di chuyển scene đến góc nhìn mong muốn  
- Chọn camera  
- Vào **GameObject → Align with View**  
- Có thể di chuyển camera như một object

---

### 📚 Essential Unity Concepts
- **Project** = Thư mục chứa toàn bộ game và asset  
- **Assets** = Model, hình ảnh, âm thanh, script, prefab...  
- **Packages** = Bộ asset được biên dịch sẵn  
- **GameObject** = Mọi đối tượng trong game, là "container" cho component  
- **Components** = Các thành phần chức năng của GameObject (Transform, Light, Script...)  
- **Scenes** = Nơi đặt GameObject để tạo level  
- **Prefab** = GameObject tái sử dụng, lưu trong Project View, có thể thêm nhiều lần vào scene  
- **Build** = Phiên bản xuất ra để chạy trên nền tảng cụ thể (Build Settings)
